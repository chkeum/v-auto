apiVersion: v1
kind: Secret
metadata:
  name: {{ vm_name }}-cloud-init
  namespace: {{ namespace }}
type: Opaque
stringData:
  userdata: |
    {% if cloud_init_content %}
    {{ cloud_init_content | indent(4) }}
    {% endif %}
